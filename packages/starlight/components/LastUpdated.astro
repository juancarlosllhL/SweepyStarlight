---
import type { Props } from '../props';

const { lastUpdated } = Astro.props;
---

{
	lastUpdated && (
		<p>
			Last updated: <time datetime={lastUpdated.toISOString()}>{lastUpdated}</time>
		</p>
	)
}
