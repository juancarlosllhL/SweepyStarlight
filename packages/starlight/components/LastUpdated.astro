---
import type { Props } from '../props';

const { lastUpdated } = Astro.props;
---

{
	lastUpdated && (
		<p>
			{'page.lastUpdated'}{' '}
			<time datetime={lastUpdated.toISOString()}>
				{lastUpdated}
			</time>
		</p>
	)
}
